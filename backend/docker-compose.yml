version: "3.7"
services:
  mongodb:
    image: mongo
    container_name: mongodb
    ports:
      - 27017:27017
  mongo-ui:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081
    depends_on:
      - mongo